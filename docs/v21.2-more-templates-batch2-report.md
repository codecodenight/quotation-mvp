# V21.2 More Templates Batch 2 Report

Date: 2026-06-22

## Scope

Added 4 category-specific quotation templates:

- 筒灯: `LED Downlight`
- 三防灯: `LED Tri-proof Light`
- 吸顶灯: `LED Ceiling Lamp`
- 太阳能壁灯: `Solar Wall Light`

Together with the existing 5 templates, the template registry now covers 9 categories.

## Implementation

- Confirmed `writeHeader(ws, template)` is already extracted to `src/lib/quote-templates/helpers.ts`.
- Added `src/lib/quote-templates/downlight.ts`.
- Added `src/lib/quote-templates/triproof.ts`.
- Added `src/lib/quote-templates/ceiling.ts`.
- Added `src/lib/quote-templates/solar-wall.ts`.
- Registered all 4 templates in `src/lib/quote-templates.ts`.
- Added workbook export tests in `src/lib/quote-export.test.ts` for all 4 new templates.

No changes were made to `src/lib/quote-export.ts` or files under `src/app/`.

## Template Notes

- 筒灯 includes Cutout and Beam Angle columns.
- 三防灯 uses Length, IP, and Luminous Efficacy columns without Material/Driver/Beam Angle.
- 吸顶灯 uses `diameter_mm` before `size_display` for Size.
- 太阳能壁灯 includes Lumens and Sensor, without Voltage/PF/Driver.

All templates reuse the existing shared helpers for param reading, suffix/prefix formatting, MOQ cleaning, carton size formatting, volume calculation, borders, and header styling.

## Verification

- `npx tsc --noEmit` passed.
- `npx vitest run src/lib/quote-export.test.ts` passed: 19 tests.
- `npx vitest run` passed: 24 files, 137 passed, 1 skipped.
