# V2.10 — Price Version Tracking Result

Generated: 2026-06-09

## Step 0 — DB Backup

- Backup created: `backups/dev-before-v2.10-price-version-20260609-231515.sqlite`
- Source Excel files were not modified, moved, renamed, or deleted.

## Step 1 — Create `price_history`

Status: COMPLETED

Actions:
- Created `price_history` in `prisma/dev.db` using raw SQLite SQL.
- Added migration file: `prisma/migrations/20260609231500_add_price_history/migration.sql`
- Updated `prisma/schema.prisma` with `PriceHistory` model and relations.
- Ran Prisma Client generation after schema update.

Verified table fields:

| Column | Type | Required | Default |
|---|---|---:|---|
| id | TEXT | yes | - |
| supplier_offer_id | TEXT | yes | - |
| old_price | DECIMAL | yes | - |
| new_price | DECIMAL | yes | - |
| old_source_file_id | TEXT | no | - |
| new_source_file_id | TEXT | no | - |
| changed_at | DATETIME | yes | CURRENT_TIMESTAMP |

Index:
- `price_history_supplier_offer_id_idx`

## Step 2 — Import Logic Upsert

Status: COMPLETED

Changed:
- Added `src/lib/supplier-offer-upsert.ts`
- Updated `src/app/import/actions.ts`

Rules implemented:
- New `product_id + factory_name` offer: create supplier offer, no price history.
- Existing offer with same price and no new CTN/MOQ data: skip.
- Existing offer with different price: write `price_history`, then update current offer price/source/timestamp.
- Existing offer missing MOQ/CTN data: fill only missing fields.
- Existing offer with MOQ/CTN already present: do not overwrite with different incoming values.

Compatibility note:
- Existing legacy `price_updated_at` values include millisecond timestamps that Prisma cannot parse as `DateTime` when returning full rows.
- `supplierOffer.create/update` now selects only `id` after writes, avoiding accidental readback of legacy timestamp values.

## Step 3 — Tests

Status: COMPLETED

Unit tests added:
- `src/lib/supplier-offer-upsert.test.ts`

Covered cases:
1. New offer -> create, no `price_history`
2. Existing offer same price -> skip, no `price_history`
3. Existing offer changed price -> update offer + write `price_history`
4. Existing offer missing CTN/MOQ -> supplement missing fields
5. Existing offer already has CTN/MOQ -> do not overwrite

Targeted test result:
- `npm test -- src/lib/supplier-offer-upsert.test.ts src/lib/hejia-import.test.ts`
- Result: 2 test files passed, 12 tests passed

## Step 4 — Dry-Run Verification

Status: COMPLETED

Dry-run method:
- Used real already-imported sample:
  - File: `sample-data/hejia/核价- Quotation- LED Solar Wall Light & Garden Light - 20240521.xlsx`
  - Sheet: `Led solar wall light`
  - Rows parsed: 14 products / 14 offers
- Ran import upsert logic inside a database transaction.
- Forced rollback at the end, so no DB rows were persisted by the verification.

Same-price re-import dry-run:

| Check | Result |
|---|---:|
| New offers | 0 |
| `price_history` rows | 0 |
| Persisted DB offer delta after rollback | 0 |

Changed-price dry-run:
- In memory only, changed the first parsed offer price by `+1`.
- Source Excel file was not modified.

| Check | Result |
|---|---:|
| Updated current offer | 1 |
| New offers | 0 |
| `price_history` rows inside transaction | 1 |
| Persisted DB offer delta after rollback | 0 |
| Persisted DB `price_history` delta after rollback | 0 |

Persisted counts after verification:

| Table | Count |
|---|---:|
| supplier_offers | 2,223 |
| price_history | 0 |

## Step 5 — Final Verification

Status: COMPLETED

Commands:

| Command | Result |
|---|---|
| `npm test` | PASS — 15 test files, 54 passed, 1 skipped |
| `npm run lint` | PASS |
| `npm run build` | PASS |

Build note:
- `npm run build` still reports the existing Turbopack dynamic file-pattern warnings from `src/lib/image-extractor.ts`.
- The build completed successfully with exit code 0.
